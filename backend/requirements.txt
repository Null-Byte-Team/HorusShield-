# HorusShield 2.0 — Python Dependencies
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Flask & Web
Flask==3.1.3
flask-socketio==5.4.1
flask-cors==6.0.0
flask-limiter==4.1.1
werkzeug>=3.0.0
requests==2.33.0

# Native Window (replaces browser popup)
# NOTE: 5.3.4 does not exist on PyPI (the 5.3.x line jumps 5.3.2 -> 5.4) —
# pinned to the last existing 5.3.x release. Verified against PyPI's index
# during the DevOps audit; `pip install -r requirements.txt` was failing on
# this line before the fix.
pywebview==5.3.2

# Network
scapy==2.7.0
psutil==6.1.1
mac-vendor-lookup==0.1.12

# AI / ML
scikit-learn==1.6.1
pandas==2.2.3
numpy==1.26.4
joblib==1.4.2

# Graph / Mesh
networkx==3.4.2

# PDF Reports
fpdf2==2.8.2
matplotlib==3.9.3

# Utilities
python-dateutil==2.9.0
colorama==0.4.6
ipwhois==1.3.0
# Explicit override: ipwhois==1.2.0 pinned dnspython<=2.0.0, which carried
# a known CVE (PYSEC-2026-1307) — found by pip-audit. ipwhois==1.3.0
# relaxes that constraint, verified compatible here.
dnspython==2.6.1
python-dotenv==1.2.2

# Optional: distributed rate limiting (only needed if HORUS_RATELIMIT_STORAGE
# is set to a redis:// URI — see app.py's graceful in-memory fallback if
# Redis isn't reachable, and docs/DEPLOYMENT.md for when to use this).
redis==5.2.1

# XML parsing hardening (defense-in-depth for parsing Nmap's XML output,
# which indirectly reflects data from scanned targets — found via a
# bandit-driven CI security pass).
defusedxml==0.7.1

# Google Sign-In (OAuth 2.0 ID token verification)
google-auth>=2.29.0

# NOTE: Do NOT install eventlet or gevent —
#       they break PyInstaller frozen EXE.
#       flask-socketio uses async_mode='threading' (no extra deps needed).
